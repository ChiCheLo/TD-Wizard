# 寶石嵌合系統 — 審查記錄

> 檔案：`design/gdd/gem-socketing.md`

---

## Review — 2026-09-20（第一次）— Verdict: NEEDS REVISION → 已修訂

Scope signal: XL
Specialists: game-designer、systems-designer、qa-lead、economy-designer、creative-director（full 模式）
Blocking items: 6 | Recommended: —
Prior verdict resolved: First review

**摘要**：首次審查。六項阻擋性問題全數當場修訂。

| # | 阻擋性問題 | 處置 |
|---|---|---|
| 1 | 介面契約未定義，下游無法實作 | 補齊跨系統介面契約 |
| 2 | `FinalDamage` 數值範圍算錯 | 重算，範圍修正為 0.1875 – 1.8 |
| 3 | `K > 0` 未強制，存在除以零風險 | 補上約束，新增 `ArmorReductionScale = 200` 與 `ArmorReduction` 換算式 |
| 4 | 缺少「換裝寶石」的驗收標準 | 補 AC |
| 5 | 融合經濟失衡（理性玩家不會融合、約 6% 玩家湊不齊材料） | `FusionCost` 3 → 2 |
| 6 | 關卡驗證規則範疇不明 | 精確化為「兩種可行屬性解，其中一種須為該進度點理論上可持有」 |

> ⚠️ **本次審查的收尾不完整**：修訂已寫入 GDD，但未建立本審查記錄、
> 未更新 `systems-index.md` 狀態、且**未同步 `entities.yaml` 的 `FusionCost`**。
> 三項缺漏於第二次審查後補正（見下）。

---

## Review — 2026-09-20（第二次）— Verdict: NEEDS REVISION → 已修訂

Scope signal: XL（6 公式、12 項上游相依全部未撰寫、跨 3 個未寫系統的介面契約、1 項需向上傳播的修訂）
Specialists: 無——本機未安裝 specialist agent 檔案，以 lean 等效模式單一審查者分析，
涵蓋 game-design / systems / QA / economy 四個視角
Blocking items: 1 | Recommended: 4
Prior verdict resolved: **無法判定**——文件標頭自稱已完成 full 審查並修訂 6 項阻擋性問題，
但磁碟上不存在本審查記錄、`systems-index.md` §7 仍顯示「待審查」，
故第二次審查將其視為首次審查處理。此歧義已由本記錄消除。

**Completeness**: 8 / 8 節齊備
**Dependency graph**: 12 項相依全部尚無 GDD，但**全部已於文件內正確自我標示為 provisional**，無隱藏的斷裂引用

### 阻擋性項目

**1. `game-concept.md` §4.3 的核心主張被本 GDD 證偽**（cross-doc）

§4.3 宣稱「限制型目標永遠可達成…此規則從根本消除了『隨機掉落 × 固定目標』的衝突，
不需額外的保底系統」。但本 GDD 的 Edge Cases 不得不發明一道保底機制——
「至少兩種可行屬性解」的關卡驗證規則——因為**限制型挑戰 × 敵人抗性佈局**
仍可能組合出實質不可達成的星星。

§4.3 的保證因此是不完整的。三星挑戰與關卡章節進程的 GDD 尚未撰寫，
若它們照字面信任 §4.3，將會漏掉自己該做的驗證。

→ **已處理**：於 §4.3 加入限定區塊，明確劃出鐵則的效力範圍僅止於挑戰目標本身，
並指明抗性佈局的可解性由關卡驗證規則負責、下游 GDD 必須實作。
反支柱 #4 的交叉引用同步更新。

### 建議修訂（4 項，全數已處理）

| # | 領域 | 問題 | 處置 |
|---|---|---|---|
| 1 | systems | 火的 3 層驅逐時機隱晦——未言明是建立時快照或持續重估 | 明確定義為**持續重估**：新層傷害高於當前最低者時，最低者立即失效；名額釋出時可重新遞補 |
| 2 | qa | 缺少火「持續時間中途被擠下」的 AC（現有 AC 只測靜態快照） | 新增中途驅逐與重新遞補的 AC |
| 3 | qa | 風的「同時命中」在實作粒度上未定義 | 定義為**同一次傷害結算批次**；批次內入列統一裁決，跨批次各自獨立。併新增跨批次 AC |
| 4 | economy | 融合的「數量 vs 品質」驗算對錯了比較基準——用「選哪顆寶石」的表去論證「要不要融合」 | 新增〈融合的取捨驗算〉小節，以真正的對照組重算：不融合 `1.5+1.5=3.0` vs 融合 `1.8+1.0=2.8`，並說明這 −6.7% 是刻意設計，融合真正勝出於站位稀缺時（呼應支柱②） |

### 審查未涵蓋、但同時發現的問題

**`entities.yaml` 的 `FusionCost` 仍為 3，與 GDD 的 2 不一致。**

第一次審查修訂 GDD 時，往登錄表補了 `ArmorReduction` 與 `ArmorReductionScale`
兩筆新項目，卻沒回頭改 `FusionCost`。

`/design-review` 的流程**不包含登錄表比對**——它比對 GDD 與 GDD，不比對 GDD 與 registry。
只有 `/design-system`（撰寫前讀取）與 `/consistency-check` 會讀 registry。
這是一個流程缺口，不是審查者的疏失。

→ **已處理**：`entities.yaml` 的 `FusionCost` 更正為 2，note 補上調降理由。

### 資深判斷

公式內部一致，六條全部重新推導無退化輸出（`FinalDamage` 0.1875 – 1.8 範圍正確、
`K > 0` 已強制、25% 護甲下限與 75% 減免上限同時防住除以零與實質免疫）。
介面契約具體到今天就能寫 stub 測試。驗收標準幾乎全是可測的 GIVEN/WHEN/THEN，
沒有「感覺平衡」這類無法驗證的敘述。

唯一實質問題是結構性的、不在本檔案內：它暴露了概念文件對挑戰可達成性的根本主張有缺口。
這正是 design-review 該在傳播下去之前抓到的東西——三星挑戰與關卡進程的 GDD 都還沒寫，
現在往上游修是最便宜的時機。

> **對 Scope Signal 的保留意見**：XL 主要反映「12 項上游相依全部尚未撰寫」，
> 這是專案階段早的結果，不是本系統本身的體量。
> 前 6 份 GDD 完成後重評預期會落回 L。排程時不應直接採用此值。

---

## 流程缺口記錄

1. **`/design-review` 不比對 entity registry。** 審查通過不代表登錄表同步。
   每次審查修訂數值後，須手動確認 `design/registry/entities.yaml`，
   或補跑 `/consistency-check`。
2. **審查後的收尾有三步，缺一不可**：寫審查記錄、更新 `systems-index.md` 狀態、
   同步 registry。第一次審查漏了全部三步。

---

## 模型改版 — 2026-09-20 — **未經審查**

觸發來源：連攜系統的設計討論（尚未撰寫 GDD）。

### 改了什麼

| 項目 | 改版前 | 改版後 |
|---|---|---|
| 剋制模型 | 敵人各自持有 `WeakAttribute` / `ResistAttribute`，手寫、需額外的互斥規則 | **五屬性相剋環** `火→霜→雷→風→蝕→火`，敵人只持有單一屬性欄位，弱點與抗性由環推導，互斥為結構自帶 |
| 敵人欄位 | `Armor`、`WeakAttribute`、`ResistAttribute` | `Armor`、`BaseAttribute`（天生，可空）、`AppliedAttribute`（外力賦予，有時效） |
| 新增介面 | — | `ApplyAttribute(attr, duration)`、`Counters(attacker, defender)` |
| 連攜的介面需求 | 檢查「起點／終點屬性」 | 檢查**配方**（屬性 × 固定數量），並寫入敵人的 `AppliedAttribute` |

### 為什麼改

連攜技能會**賦予敵人屬性**——例如「炎寒領域」第一階段的暴風雪賦予霜屬性，
第二階段的火海再以火剋霜打出剋制傷害。舊模型只能表達「這隻敵人對某屬性弱」，
無法表達「火剋霜」這種元素與元素之間的關係，撐不起這個機制。

### 影響範圍

- Detailed Design：新增〈屬性相剋環〉章節
- 介面契約：`IAttributeTarget` 改寫，新增相剋環查詢
- Edge Cases：〈屬性剋制〉整段改寫，新增賦予屬性的時效邊界情況
- 驗收標準：依賴敵人屬性欄位的 AC 改寫
- `entities.yaml`：新增 `AffinityRing`，三個倍率常數的 note 改寫
- 上游 `game-concept.md` §3.2：連攜機制整段重寫（鏈路模型 → 配方＋領域模型）
- 上游 `art-bible.md` §7：連攜提示整段重寫（鏈路預覽作廢 → 落點圓）

### ⚠️ 下次審查的重點

1. **相剋環的擴充性**：MVP 5 屬性剛好成環。v1.0 擴到 12–15 種時，環的形狀怎麼辦？
   多環？分組？這個問題目前無解答。
2. **範圍擴張的後果**：相剋環對**每一次塔的攻擊**生效，不只連攜。
   這使關卡的敵人屬性配置直接決定「該帶哪些寶石」，
   與「敵人屬性 × 挑戰禁用項須留下兩種可行解」的驗證規則交互作用變複雜。
3. **`AppliedAttribute` 覆寫語意**：目前定義為完全覆寫天生屬性。
   這意味著可以把敵人染成**對自己有利**的屬性，也可能被玩家用來
   規避某些敵人的天生優勢——是否為預期的策略深度，待驗證。
4. **時效與階段時長的耦合**：連攜後段階段必須早於前段賦予的時效終點，
   否則吃不到剋制。這條約束目前寫在 Edge Cases，但真正該由連攜系統 GDD 保證。

---

## Review — 2026-09-20（第三次）— Verdict: NEEDS REVISION → 已修訂

Scope signal: L
Specialists: game-designer、systems-designer、qa-lead、economy-designer、level-designer、creative-director
Blocking items: 11（**2 項駁回**）| Recommended: 9
Prior verdict resolved: Yes — 前兩輪的項目皆已處理；本輪針對相剋環改版

### ⚠️ 駁回的兩項（前提有誤）

**駁回 1 — 「剋制環從連攜限定偷偷擴大成每次攻擊生效」**

這是本輪審查的**核心論點**，也是給 producer 那句話的基礎。它不成立。

舊模型的 `WeakAttribute` 同樣對**任何塔攻擊**觸發 ×1.5，公式節明寫「所有攻擊皆經過此公式」。
改版改的是**結構**（環 vs 逐敵人手寫欄位），不是**範圍**。

審查者看到的 ⚠️ 註記是撰寫時加的**澄清**，被誤讀為變更。

> **但這個誤讀撞到了一個真問題**：舊模型兩個屬性欄位皆可空，
> 設計師能讓大量敵人完全中性——**暴露率是可控的**。
> 相剋環若不明定暴露率，會被理解成「每隻敵人都有屬性」。
> 已新增〈屬性暴露率〉一節定案：**多數敵人 `BaseAttribute = null`，約三成持有屬性**。

**駁回 2 — economy-designer「`C` 的 flat fee 結構性問題」**

原文：「早期便宜塔的絕對金額小到不構成決策，後期昂貴塔的絕對金額大到玩家不敢重配」。

兩個錯誤：
1. **本作只有一種基礎塔**（反支柱②：不做多種塔型）。不存在塔的價格階梯，
   「早期便宜塔／後期昂貴塔」這個前提在本作不存在。
2. `C` 定義為**建造費的 60%**，本身就是比例，不是 flat fee。

已於〈嵌合經濟模型〉加註，避免日後重複提出。

### 降級的一項

**economy-designer「MVP 內融合能否實際發生，未經驗證」** → 降為已解決。

供給面可由鴿籠原理直接證明：3 初始 + 2×6 關 = 15 顆，12 顆隨機落在 5 種屬性上，
12 > 5×2，**必然**有某屬性達到 2 顆的融合門檻。材料湊得齊是數學保證。

真正待驗證的是「玩家願不願意融合」，已併入 Open Question #9。
已新增〈融合材料的供給驗算〉小節。

### 已修訂的阻擋性項目（9 項）

| # | 來源 | 項目 | 處置 |
|---|---|---|---|
| 1 | systems-designer | `entities.yaml` 的 `FinalDamage.output_range` 仍是舊值 0.5625 | 更正為 0.1875，並加註推導 |
| 2 | creative-director | systems-index 徽章顯示「已審查通過」與敘述矛盾 | 徽章改為 NEEDS REVISION，進度改為 0/34，並加註「兩處必須同步」 |
| 3 | creative-director | art-bible §5 敵人段落殘留舊模型語言 | 改寫為屬性符文，並補 `BaseAttribute` vs `AppliedAttribute` 的視覺區分規格 |
| 4 | game-designer | 剋制環的決策驅動力 | 定案為**調味非主菜**，並明文禁止為此調高 `WeaknessMult`（會破壞屬性壓過等級的驗算） |
| 5 | game-designer + level-designer | 環的二元結構讓配置收斂成查表 | 由「多數敵人無屬性」的暴露率定案緩解 |
| 6 | systems-designer + creative-director | 批次邊界無中央調度器 | 新增〈對傷害計算系統的硬性契約〉，明寫「在該調度器存在前，火疊層與風擊退無法寫出可信的 stub」，並登記於 systems-index §8 |
| 7 | qa-lead | `AppliedAttribute` 時效行為完全無 AC | 新增 7 條 AC（時效歸零回歸、同屬性刷新、異屬性覆寫、連續賦予、null 呼叫、同屬性中立） |
| 8 | qa-lead + level-designer | 關卡驗證 AC 是敘述非斷言 | 整段重寫：定義三項輸入的型別與來源（含 `cumulativeDropPool` 的具體定義）、「可行解」的形式化定義、4 條具體 GIVEN/WHEN/THEN，並明示宿主是關卡與章節進程系統 |
| 9 | economy-designer | 融合 −6.7% 的比較基準被支柱②否定 | 同意。框架反轉——**稀缺是常態，融合多數情況下該做**；−6.7% 是「攤得開就別融」的下界反例，不是主結論 |

### 已修訂的 Recommended

- `IAttributeTarget` 新增 `AppliedAttributeRemaining`（Visual Requirements 的「到期閃爍」硬需求）
- 穿甲改為 `IReadOnlyList<PenetrationStack>` 獨立疊加清單，理由：各筆時效不同須能個別到期
- `Counters()` 加註「MVP 專用簽章，預期改動」，並說明 v1.0 可能改為回傳倍率或接受環 ID
- 火的「打聚在一起的」加註：描述的是**結果**不是智慧索敵，實作不得加入找密集處的邏輯

### 未處理（承認為缺口）

- **相剋環在 v1.0 擴到 12–15 種屬性時無解**。已列 systems-index §9 第 8 條，MVP 資料結構須預留。
- **level-designer「單關該混幾種敵人屬性」的設計指引**：屬關卡與章節進程系統職責，本 GDD 不越界。
- **`AppliedAttribute` 完全覆寫在 v1.0 的繞環風險**：已列 systems-index §9 第 10 條。

### 對本輪審查方法的一點記錄

本輪 11 項阻擋中有 2 項前提錯誤，且其中一項是整份審查的**核心論點與 producer 摘要的基礎**。
兩項錯誤都源於**未回頭核對已定案的上游約束**（舊模型的實際範圍、反支柱②只有一種塔）。

> **教訓**：審查產出的 blocking 項目需逐條核對前提，不可整份照單全收。
> 但同樣重要的是——本輪 3 項機械性缺失（登錄表舊值、徽章矛盾、art-bible 殘留）
> **全部屬實**，而那 3 項正是撰寫者最容易漏掉的類型。審查的價值在此。

---

## Review — 2026-09-20（第四次，複審）— Verdict: APPROVED

Scope signal: L（producer 應在前 6 份 GDD 完成後重評）
Specialists: 無（`--depth lean`）
Blocking items: 0 | Recommended: 1
Prior verdict resolved: Yes — 第 3 輪的 9 項阻擋修訂與 2 項駁回，本輪逐一覆核

**本輪特別任務**：不採信第 3 輪審查記錄撰寫者的說法，獨立回頭核對
game-concept.md 反支柱與 gem-socketing.md 公式節，驗證「駁回的兩項」前提是否成立。

**駁回 2（`C` 的 flat fee 結構性問題）—— 查核結果：前提成立**。
game-concept.md §2 反支柱②逐字為「不做多種塔型」，與 gem-socketing.md 引用一致；
Tuning Knobs 表確認 `C` 定義為「建造費的 60%」，本身即為比例而非固定金額。
兩項事實可在文件中直接對上，證據鏈完整。

**駁回 1（剋制環範圍從連攜限定擴大成每次攻擊生效）—— 查核結果：前提成立，但有一處無法完全獨立驗證**。
公式節「所有攻擊皆經過此公式」確認現行模型對每次攻擊生效；「模型改版」變更紀錄
未列出任何「觸發範圍」曾經改變；systems-index §8 將此列為既定事實而非變更警示。
三項間接證據方向一致、無反證，但因專案非 git 倉庫、舊模型原文已被覆寫，
無法逐字比對舊版本，此點屬間接推論而非直接文件證據。

**結論**：兩項駁回皆站得住腳，第 3 輪的判斷未被推翻。

### Recommended（1 項）

- 建議在第 3 輪審查記錄的「駁回1」下方補一句限制說明，載明此結論基於間接證據
  （模型改版記錄未列出範圍變更、現行公式節措辭、systems-index 的既定事實式登記），
  而非逐字比對舊版本原文，避免未來審查者誤判為有完整文件實證的鐵案。

### 資深判斷

公式六條重新驗算全數正確。兩項駁回經獨立查證後皆合理，且駁回過程中撞到的真問題
（屬性暴露率）已另外定案處理，未因駁回而遺漏。無新增阻擋項。

### Verdict: **APPROVED**
